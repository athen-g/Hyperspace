import React from 'react'
import { BackgroundPathsDemo } from './ui/BackgroundPaths'

const Contact = () => {
  return (
    <section id="contact" className="blog-hero">
      <div className="hero-name">
        <span className="hero-title">CONTACT</span>
        <span className="hero-subtitle">Get in touch</span>
      </div>
    </section>
  )
}

export default Contact
