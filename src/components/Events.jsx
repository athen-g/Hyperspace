import React from 'react'
import { BackgroundPathsDemo } from './ui/BackgroundPaths'

const Events = () => {
  return (
    <section id="events" className="blog-hero">
      <div className="hero-name">
        <span className="hero-title">EVENTS</span>
      </div>
    </section>
  )
}

export default Events