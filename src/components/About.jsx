import React from 'react'
import { BackgroundPathsDemo } from './ui/BackgroundPaths'

const About = () => {
    return (
        <section id="about" className="blog-hero">
            <div className="hero-name">
                <span className="hero-title">ABOUT</span>
            </div>
        </section>
    )
}

export default About